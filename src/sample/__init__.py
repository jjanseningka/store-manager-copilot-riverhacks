"""Sample package shipped with the starter scaffold.

Rename this package and replace its contents when you fork the starter into
a real project. The package exists so `make test`, `make lint`, and
`make typecheck` have something to run against on day one.
"""

from sample.greeter import greet

__all__ = ["greet"]
